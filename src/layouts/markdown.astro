---
import Layout from "./layout.astro"

const { frontmatter } = Astro.props;
---

<Layout title={frontmatter.title + " - Marcus Felix"}>
  <div class="container mx-auto max-w-2xl py-20 prose-lg">
    <slot/>
  </div>
</Layout>
