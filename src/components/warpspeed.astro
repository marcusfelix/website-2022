<canvas id="canvas" class="w-full h-full"></canvas>
<script is:inline src="/warpspeed.js"></script>
<script>
const warp = new WarpSpeed("canvas", {
  density: 2,
  speed: 8,
  starSize: 10,
  warpEffectLength: 1,
  backgroundColor: "#420f0f",
  starColor: "#fc7979"
})

addEventListener("scroll", (event) => {
  warp.SPEED = window.scrollY / 4
});
</script>